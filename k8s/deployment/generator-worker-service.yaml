apiVersion: v1
kind: Service
metadata:
  name: generator-worker-service
spec:
  selector:
    app: generator-worker
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
